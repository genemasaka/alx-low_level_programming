#include "main.h"

/**
*main - function that prints a string to 
*stdout
*Return: Always (0)
*/
int main(void)
{
char *str;

str = "I do not fear computers. I fear the lack of them - Isaac Asimovi";
_puts(str);
return (0);
}
